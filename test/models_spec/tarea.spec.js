const assert = require('mocha').assert
const tarea = require('../../dist/models/tarea');

describe('Tarea', function() {
    it(' Tarea es una clase', function() {

        assert.instanceOf(tarea, constructor, "[message]");;
    })
})